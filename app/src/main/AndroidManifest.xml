<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.blikoon.qrcodescannerlibrary"
    android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        tools:replace="icon, label, roundIcon"
        android:icon="@mipmap/chs_store"
        android:label="CHS Store"
        android:roundIcon="@mipmap/chs_store"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".SellActivity" />
        <activity android:name=".ReturnActivity" />
        <activity android:name=".RestockActivity" />
        <activity android:name=".AddProductActivity" />
        <activity android:name=".ModifySalePriceActivity" />
        <activity android:name=".ViewInventoryActivity" />
        <activity android:name=".OpeningActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>